
spring:
  datasource:
    password: ok
    url: jdbc:postgresql://localhost:5432/serverdb
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
      show_sql: true
